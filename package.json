{
  "name": "mates-app",
  "version": "1.0.0",
  "description": "Maps of your dates - A shared places app",
  "scripts": {
    "build": "mkdir -p public && cp -r *.html *.js *.css *.json *.rules public/",
    "deploy": "npm run build && firebase deploy --only hosting,firestore,storage",
    "serve": "firebase serve"
  },
  "dependencies": {
    "firebase": "^11.6.0"
  },
  "devDependencies": {
    "firebase-tools": "^12.4.4"
  },
  "type": "module"
}
